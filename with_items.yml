---
- hosts: 2nd-group
  connection: ssh
  gather_facts: yes
  
  
  tasks: 
    - name: remove packages
       yum: pkg={{item} state=absent update_cache=true
       with_items:
          - sysstat
          - sendmail
          - vim
          - nfs
          - ntp
          
 
